<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.4</version>
        <relativePath/>
    </parent>

    <groupId>ci.itechciv.sigdep</groupId>
    <artifactId>sigdep-sync</artifactId>
    <!-- Version pilotée par le build : la CI passe -Drevision=<tag> (sans le
         'v') sur le tag de release, de sorte que le JAR packagé (finalName,
         Implementation-Version du manifest, /actuator/info) porte la MÊME
         version que le tag Git et l'image Docker. En dev, revision reflète la
         prochaine release. Le flatten-maven-plugin résout ${revision} dans le
         POM installé (sinon la property fuite dans les artefacts). -->
    <version>${revision}</version>
    <packaging>jar</packaging>

    <name>sigdep-sync</name>
    <description>Edge agent deployed on each SIGDEP-3 site. Reads OpenMRS MySQL read-only, buffers in SQLite, pushes to sigdep-hub.</description>

    <properties>
        <java.version>17</java.version>
        <revision>2.2.0-SNAPSHOT</revision>
        <!-- SHA git court, injecté par la CI (-Dgit.sha=...). 'unknown' en
             build local où le commit exact importe peu. -->
        <git.sha>unknown</git.sha>
        <sigdep-contracts.version>0.1.0-SNAPSHOT</sigdep-contracts.version>
        <sqlite-jdbc.version>3.46.1.0</sqlite-jdbc.version>
        <okhttp.version>4.12.0</okhttp.version>
        <resilience4j.version>2.2.0</resilience4j.version>
        <spring-dotenv.version>4.0.0</spring-dotenv.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>ci.itechciv.sigdep</groupId>
            <artifactId>sigdep-contracts</artifactId>
            <version>${sigdep-contracts.version}</version>
        </dependency>

        <dependency>
            <groupId>me.paulschwarz</groupId>
            <artifactId>spring-dotenv</artifactId>
            <version>${spring-dotenv.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>${sqlite-jdbc.version}</version>
        </dependency>

        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
            <version>${okhttp.version}</version>
        </dependency>

        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-retry</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-circuitbreaker</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>1.20.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mysql</artifactId>
            <version>1.20.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <executable>true</executable>
                </configuration>
                <executions>
                    <!-- Génère META-INF/build-info.properties (version + date
                         de build + SHA git). Source de vérité pour le log de
                         démarrage. Le SHA vient de -Dgit.sha=<sha> passé par la
                         CI ; en local il retombe sur 'unknown'. -->
                    <execution>
                        <id>build-info</id>
                        <goals><goal>build-info</goal></goals>
                        <configuration>
                            <additionalProperties>
                                <git.sha>${git.sha}</git.sha>
                            </additionalProperties>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Tests d'intégration (*IT) : exécutés par failsafe en phase
                 `verify`, PAS par surefire en phase `test`. `mvn test` reste
                 donc purement unitaire (rapide, sans Docker) ; `mvn verify`
                 lance en plus les *IT, qui démarrent des conteneurs MySQL via
                 testcontainers (Docker requis — présent en CI). Sans ce plugin,
                 les *IT n'étaient JAMAIS exécutés (surefire ne matche que
                 *Test/Test*/*Tests) : ils existaient sans jamais tourner.
                 Le pattern par défaut de failsafe (*IT, IT*, *ITCase) couvre
                 nos fichiers, d'où l'absence de <includes>. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <id>integration-tests</id>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- Résout ${revision} dans le POM installé/packagé (pattern
                 officiel d'une version pilotée par -Drevision). Sans ça,
                 ${revision} fuite littéralement dans les artefacts. -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>1.6.0</version>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>resolveCiFriendliesOnly</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals><goal>flatten</goal></goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals><goal>clean</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
